import * as authentication from "@dfinity/authentication";

// Implements feature requirements for #IC-130
describe("Authenticate calls to the IDP Canister", () => {
  it.todo("should derive caller from URL passed to register");

  it.todo("should gracefully handle malformed / insufficient URLs");

  it.todo("should authenticate calls to agent to add");
  it.todo("should authenticate calls to agent to remove");

  it.todo("should not pass authentication for calls to list");

  it.todo("should not pass authentication for calls to query");
});
