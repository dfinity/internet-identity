import IDPActor from "../utils/actor";

describe("Actor Interface", () => {
  it.todo("should handle adding a new identity");

  it.todo("should handle removing an identity");

  it.todo("should handle looking up stored info for a user");
});
