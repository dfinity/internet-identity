import credentialRecovery from "../credentialRecovery";

// Implements requirements for #IC-138
describe("Credential Recovery", () => {
  it.todo("should check localStorage for a credentialId");

  it.todo("should gracefully handle failing to request a credentialId");
  it.todo("should request a credentialId from the server");

  it.todo("should store a credentialId in localStorage after fetching");
});
