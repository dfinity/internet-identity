---
import Layout from "../layouts/Layout.astro";

const { title, pageName } = Astro.props;
---

<Layout title={title}>
  <main id="pageContent" aria-live="polite" data-page-name={pageName}></main>
  <div id="loaderContainer"></div>
  <slot />
</Layout>