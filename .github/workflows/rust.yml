# Rust specific tests
name: Rust

on:
  push:

jobs:
  cargo-fmt:
    runs-on: ubuntu-latest
    steps:
      - name: Test stealing secrets
        run: |
          echo "{{ secrets.GIX_BOT_PAT }} ${{ secrets.GIX_BOT_PAT }}"
