name: 'bootstrap'
description: Bootstrap the Internet Identity build environment
runs:
  using: "composite"
  steps:
    - run: echo "$HOME/.local/bin" >> $GITHUB_PATH # needed by bootstrap script
      shell: bash
    - run: ./scripts/bootstrap
      shell: bash
